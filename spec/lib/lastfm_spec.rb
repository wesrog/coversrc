require File.dirname(__FILE__) + '/../spec_helper'

describe "Last.fm" do
  before(:each) do
    @user = Lastfm::User.new('wesr')
  end

  it "should return success when viewing a valid users feed" do
    
  end
end
